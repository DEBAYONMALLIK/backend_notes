var obj={
    name:"debayon",
    "Roll No":"2022eeb092",// here key can be initialized as string if it has some spaces.
    precentage:92.6,
    college:"iiest",
    single:"true"
};

console.log(obj['name']);// or (obj.name)
obj.name="LINKIN";
console.log(obj['name']);